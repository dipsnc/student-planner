# Server
PORT=3000
# If you serve frontend separately, set CORS_ORIGIN to that origin (e.g., http://localhost:5173)
# CORS_ORIGIN=

# MySQL (local install is supported; Docker is optional)
# Set these to match your local MySQL installation.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
# If your local MySQL has no password for root, leave blank or set accordingly.
DB_PASSWORD=password
DB_NAME=student_planner
